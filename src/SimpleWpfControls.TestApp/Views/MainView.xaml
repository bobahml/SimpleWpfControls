<Window x:Class="SimpleWpfControls.TestApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:view="clr-namespace:SimpleWpfControls.TestApp.LoadingIndicator.View"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:SimpleWpfControls.TestApp.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="350" Width="525" d:DataContext="{d:DesignInstance viewModels:MainViewModel}">
    <Grid>
        <TabControl>
            <TabItem Header="LoadingIndicatorTest">
                <view:LoadingIndicatorTestView DataContext="{Binding LoadingIndicatorTestViewModel}" />
            </TabItem>
        </TabControl>

    </Grid>
</Window>
